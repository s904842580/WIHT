# 后端目录

后端是 WAHT 的当前主线。

## 目录规划

```text
backend/
  waht-java/       # [NOW] Spring Boot 3 主后端
  waht-go/         # [LATER] Go 辅助服务
  ai-service/      # [LATER] AI 助手服务
```

## 当前规则

- 当前只做 `waht-java`。
- `waht-go` 只留说明，不写代码。
- `ai-service` 只留说明，不写代码。
- 每个 Java 文件尽量写清楚注释，方便学习复盘。
